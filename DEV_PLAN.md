# 2026 世界杯项目 — 开发进度分析与后续计划

**分析日期:** 2026-05-28

---

## 一、当前进度总览

### 已完成的部分

项目目前是一个 **Next.js 14 赛程日历订阅工具**，具备以下功能：

| 模块 | 状态 | 说明 |
|------|------|------|
| 赛程数据解析 | 已完成 | ICS 日历解析，支持 48 支球队识别 |
| 分组积分榜 | 已完成 | GroupStandings 组件，支持小组赛排名展示 |
| 比赛卡片 | 已完成 | MatchCard、MatchStats、MatchFilters 组件 |
| 世界杯首页 | 已完成 | WorldCupHero 倒计时/进度组件 |
| 日历订阅 | 已完成 | ICS 下载 + webcal 订阅链接 |
| 导航栏 | 已完成 | NavBar + MobileNavBar 响应式 |
| 仪表盘外壳 | 已完成 | DashboardShell 布局容器 |
| 球队数据 | 已完成 | 48 支球队中英文映射、国旗 emoji 解析 |

### 尚未开始的部分（手册中描述的 Prediction OS）

DEVELOPMENT_HANDBOOK.md 和 SYSTEM_ARCHITECTURE.md 描述了一个完全不同的产品愿景——**实时预测市场终端**（World Cup Prediction OS）。该系统包含：

- **Module A（概率地图）**：deck.gl WebGL 地图，粒子系统，能量流线
- **Module B（排名流动）**：DOM + framer-motion 弹簧动画排名榜
- **Module C（赔率时间线）**：Canvas 2D 多线图，Catmull-Rom 样条
- **Module D（实时滚动条）**：DOM + rAF 横向滚动条
- **Zustand 状态管理**：7 个 store slice
- **后端服务**：Polymarket WebSocket 数据源、差值引擎、事件检测器

这些内容在当前代码库中 **零实现**。

---

## 二、关键决策点

在制定后续计划之前，需要明确一个核心问题：

**当前项目（赛程日历）和手册描述的项目（预测市场终端）是两个不同的产品。**

- 赛程日历：信息展示型工具，用户订阅比赛日程
- 预测市场终端：实时数据仪表盘，展示 Polymarket 赔率

两者可以独立存在，也可以融合。融合的思路是：在赛程日历基础上增加预测市场数据，让每场比赛不仅展示时间地点，还展示各队的夺冠/晋级概率。

---

## 三、后续开发计划

### 路线 A：渐进式增强（推荐）

在现有赛程应用基础上逐步引入预测市场功能，降低技术风险。

#### 第一阶段：数据层搭建（3-4 天）

| 任务 | 优先级 | 说明 |
|------|--------|------|
| 引入 Zustand 状态管理 | 高 | 替代当前 useState，为后续模块间通信做准备 |
| 设计 CountryData 类型 | 高 | 复用手册中的类型定义，但适配 Next.js 环境 |
| 创建 Mock 数据层 | 高 | 48 支球队的模拟概率数据，无需后端即可开发 UI |
| 实现 WebSocket 服务桩 | 中 | 日志打印模式，验证数据管道 |
| 创建历史数据缓冲区 | 中 | 内存中存储时间序列数据 |

#### 第二阶段：Module D — 实时滚动条（2-3 天）

从最简单的模块开始，验证 store → UI 管道：

| 任务 | 说明 |
|------|------|
| TickerItem 组件 | 国旗 + 队名 + 概率值 + 变化量 + 方向条 + 成交量 |
| 无缝循环滚动 | rAF + transform: translateX()，非 CSS animation |
| 左侧停靠区 | LIVE 指示灯 + 时间戳 |
| 右侧停靠区 | 暂停按钮 + 筛选下拉菜单 |
| 点击选择 | 点击条目触发 store.selection 联动 |

#### 第三阶段：Module B — 排名流动（3-4 天）

DOM 模块，验证动画管道：

| 任务 | 说明 |
|------|------|
| RankingRow 组件 | 排名编号 + 国旗 + 队名 + 概率值 + 数据条 + 迷你趋势图 |
| 弹簧动画 | framer-motion spring transition（stiffness: 200, damping: 24） |
| 条纹器效果 | 排名编号和概率值的数字滚动动画 |
| 挤压机制 | 概率接近时的间距压缩 |
| 振动反馈 | 概率变化 >1% 时的水平抖动 |
| 前三名领奖台效果 | 金/银/铜色高亮 |
| 详情覆盖面板 | 滑入面板展示历史图表和统计数据 |

#### 第四阶段：Module C — 赔率时间线（3-4 天）

Canvas 2D 模块，最复杂的渲染逻辑：

| 任务 | 说明 |
|------|------|
| Canvas 2D 画布 | TimelineCanvas 组件 |
| 轴线渲染器 | 网格线 + 轴标签 + 动态缩放 |
| 多线渲染器 | Catmull-Rom 样条 + 多层辉光 + 线宽分级 |
| 加速度颜色调制 | 每段线条颜色随变化速度变化 |
| 时间预设 | 1H / 24H / 7D / 30D 切换 |
| 平移和缩放 | 拖拽平移 + 滚轮缩放 |
| 十字准线 + 工具提示 | 悬停显示精确数值 |
| 事件标记 | 菱形标记 + 引导线 + 详情提示 |
| 单国聚焦模式 | 点击隔离单条线 |

#### 第五阶段：Module A — 概率地图（4-5 天）

技术难度最高，最后实现：

| 任务 | 说明 |
|------|------|
| GeoJSON 加载 | Natural Earth 110m 简化版 |
| deck.gl 画布 | DeckGL 组件封装 |
| 国家多边形层 | 按概率填充颜色 |
| 辉光渲染 | 离屏 FBO + 3 通道高斯模糊 |
| 呼吸动画 | shader uniform + 3.2s 正弦波 |
| 粒子系统 | Float32Array + 1200 粒子 |
| 能量流线 | 12 条最强流向 + 贝塞尔曲线 + 虚线动画 |
| 悬停交互 | GPU pick buffer + 聚光灯变暗 + 工具提示 |
| 投影切换 | 地球仪 / 平面切换 |

#### 第六阶段：集成与后端（3-4 天）

| 任务 | 说明 |
|------|------|
| DashboardGrid | react-grid-layout 容器 |
| 搜索面板 | Cmd+K 模糊搜索 |
| 状态栏 | 连接状态 + 总成交量 + 最后更新时间 |
| 布局持久化 | localStorage 存储 |
| URL 深度链接 | hash → selection 状态 |
| 跨模块联动 | 点击地图 → 排名滚动 → 时间线聚焦 → 滚动条高亮 |
| 后端 WebSocket | Polymarket CLOB 连接 |
| 差值引擎 | 1m/5m/1h/24h 概率差值计算 |
| 事件检测器 | 阈值/突增/成交量激增检测 |

#### 第七阶段：打磨（2-3 天）

| 任务 | 说明 |
|------|------|
| 性能审计 | Lighthouse + React Profiler，目标 60fps |
| 无障碍审计 | 键盘导航 + 屏幕阅读器 + 颜色对比度 |
| 加载骨架屏 | 每个模块的 Skeleton 组件 |
| 错误边界 | 每个模块独立 ErrorBoundary |
| Favicon + meta 标签 | PWA manifest |
| 包体积优化 | <250KB gzipped |
| 跨浏览器测试 | Chrome / Firefox / Safari / Edge |
| 移动端适配 | 响应式断点 |

### 路线 B：全新重建

如果决定完全按照手册描述的 Vite + Zustand 架构从零构建，则需要：

1. 新建 Vite 项目，安装手册中的所有依赖
2. 按照手册 Phase 0-8 的顺序依次实现
3. 现有赛程应用可作为独立页面保留

这条路线工作量更大，但架构更纯净。

---

## 四、技术栈对比

| 维度 | 当前（赛程） | 手册（Prediction OS） |
|------|-------------|----------------------|
| 框架 | Next.js 14 | Vite + React |
| 状态管理 | useState | Zustand（7 slices） |
| 样式 | Tailwind CSS | Tailwind CSS |
| 动画 | framer-motion | framer-motion + rAF + Canvas |
| 地图 | 无 | deck.gl (WebGL) |
| 图表 | 无 | Canvas 2D 自绘 |
| 后端 | 无 | Bun + WebSocket |
| 数据源 | ICS 日历文件 | Polymarket API |

---

## 五、建议的下一步

1. **确认产品方向**：是融合两个产品，还是只做预测市场终端？
2. **如果融合**：先完成第一阶段（数据层），然后按 Module D → B → C → A 的顺序逐步构建
3. **如果全新重建**：按照手册 Phase 0 开始，用 Vite 脚手架替换 Next.js
4. **优先获取 Polymarket API Key**：后端开发需要
5. **准备 GeoJSON 数据**：下载 Natural Earth 110m 并用 mapshaper 简化

预计总工期：融合路线约 **20-25 个工作日**，全新重建约 **14-16 个工作日**（按手册估算）。
